<!doctype html>
<html lang="es">
<head>
	<meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
	<link href="css/styles.css" type="text/css" rel="stylesheet"/>
  <link rel="shortcut icon" href="img/icon/favicon.png" type="image/x-icon">
  <title>Amigo Invisible</title>
</head>
<body>
	<header>
		<h1>Amigo Invisible</h1>
		<nav>
			<ul>
				{{#if user}}
				<li><a href="/">Home</a></li>
				<li><a href="perfil">{{user.username}}</a></li>
				<li><a href="mensajes">Mensajes</a></li>
				<li><a href="logout">Desconectarte</a></li>
				{{else}}
				<li><a href="login">Iniciar Sesión</a></li>
				<li><a href="register">Registrarte</a></li>
				{{/if}}
			</ul>
		</nav>
	</header>	
		<div>
      <div>
        {{#if success_msg}}
          <div>{{success_msg}}</div>
        {{/if}}

        {{#if error_msg}}
          <div>{{error_msg}}</div>
        {{/if}}

        {{#if error}}
          <div>{{error}}</div>
        {{/if}}
          
        {{{body}}}
      </div>
    </div>
<!-- 	<script src="js/sorteo.js" type="text/javascript"></script -->
</body>